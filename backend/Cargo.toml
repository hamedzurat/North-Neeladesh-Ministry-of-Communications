[package]
name = "backend"
version = "0.1.0"
edition = "2024"

[lib]
name = "backend"
path = "src/lib.rs"

[dependencies]
cpal = { version = "0.18", default-features = false, features = ["pipewire"] }
serde_json = "1"

[[bin]]
name = "backend"
path = "src/main.rs"
