[package]
name = "exchange-protocol-harness"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
exchange-backend = { path = "../backend" }
exchange-protocol = { path = "../protocol" }
